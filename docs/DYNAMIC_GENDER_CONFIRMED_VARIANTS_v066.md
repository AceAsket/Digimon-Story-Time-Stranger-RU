# Подтверждённые динамические формы рода v066

Production-набор `exports/dynamic_gender_confirmed_variants_v066.csv` содержит 23 базовых ID: 13 реплик игрока и 10 реплик Оператора. Они включены в Lua-карту общего Message-hook; фактические runtime key и технические группы перечислены в `exports/dynamic_gender_runtime_manifest_v067.csv`.

Ещё 28 подтверждённых реплик игрока из Digimon Chat вынесены в `exports/dynamic_gender_chat_unhooked_variants_v066.csv`. Общий итог аудита — 51 базовый ID: 41 реплика игрока и 10 реплик Оператора. Строки чата помечены `basis=player_self_chat_unhooked`: их нельзя включать в production-карту, пока не подтверждено, что Digimon Chat проходит через тот же Lua-resolver. Без отдельного перехвата они должны остаться нейтральными.

Колонки `male_protagonist_text` и `female_protagonist_text` всегда описывают выбранного героя, а не пол говорящего. Поэтому для `role=operator` форма самого Оператора инвертирована:

- герой-мужчина → Оператор-женщина;
- герой-женщина → Оператор-мужчина.

Для строки `s910_169_640` меняется обращение Оператора к герою. Текст по-прежнему кладётся в инвертированную ветку Оператора: герой-мужчина выбирает `__F`, герой-женщина — `__H`.

Speaker ID копируется из базовой MBE-строки (`char_PLAYER_M` или `char_OPERATOR_M`) и не заменяется вручную. Во всех исходных message-таблицах используются именно эти базовые ID; движок сам переключает отображаемого героя/Оператора. Принудительные `char_PLAYER_F`/`char_OPERATOR_F` в вариантах могли бы обойти штатную логику и потому не применяются.

Суффиксы `_M` и `_F` из четвёртого столбца MBE не использованы как текстовые ID. Они являются voice/event aliases. Реализация создаёт новые строки `<base_id>__H` и `<base_id>__F`, а Lua-таблица выбирает их по `GetGender()`.

Двенадцать родовых реплик являются вариантами ответа внутри трёхстрочных `MessageTalkSel`-групп. Для них движок получает ID первой строки группы, а не ID конкретного ответа. Поэтому генератор создаёт две полные технические копии группы в исходном порядке и меняет только подтверждённую родовую строку. Остальные 11 runtime key являются прямыми сообщениями.

Все строки сверены с текущими CSV и английским оригиналом Steam build 23514637. Текстовые управляющие теги (`{next}`) сохранены. В нескольких строках одновременно устранены очевидные механические формулировки: `Можете поспорить`, `ходил сюда`, `стал свидетелем аномального феномена`, `нечто Среднее`.

Нейтральные и естественно звучащие реплики в набор намеренно не включались: им не нужны две физические строки. Также исключены формы, относящиеся к собеседнику-дигимону или третьему лицу.
